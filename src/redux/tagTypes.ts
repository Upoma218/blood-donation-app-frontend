export enum tagTypes {
  donors = "donors",
  admin = "admin",
  requester = "requester",
  user = "user",
  users = "users",
  requestData = "requestData",
  request = "request",
  status = "status",
  stats = "stats",
}

export const tagTypesList = [
  tagTypes.donors,
  tagTypes.admin,
  tagTypes.requester,
  tagTypes.user,
  tagTypes.requestData,
  tagTypes.request,
  tagTypes.status,
  tagTypes.stats,
];
